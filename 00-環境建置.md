---
title: 'Claude Code 懶人包 #00：環境建置'
date: '2026-04-04'
type: 懶人包
version: v0.3
status: 初版（實作後更新）
tags:
  - Claude-Code
  - 懶人包
  - 環境建置
  - uv
video: EP03
---
# Claude Code 懶人包 #00：環境建置

> 版本：v0.4
> 更新日期：2026-06-12
> 對應影片：Claude基本功 EP03

> 📌 **本懶人包可獨立執行**：會自動檢查並安裝所需工具，不需要先看過其他懶人包。你只要確認下方「先備條件」即可開始。

---

## 這個懶人包會幫你做什麼？

幫你把使用 Claude Code 懶人包所需的所有環境一次建置完成，包括：
- 確認 Claude Code 桌面版正常運作
- 安裝 uv 等基礎工具
- 驗證所有工具都能正常使用

> 💡 **本懶人包只負責安裝基礎工具。** Git、GitHub CLI、GitHub 帳號登入等需求，請接著執行 **懶人包 #02：連接 GitHub**（內含完整 Git/GitHub CLI 安裝與帳號註冊引導）。

---

## 先備條件

在使用這個懶人包之前，請確認：

- [ ] 已有 Claude 帳號（Pro 方案以上）
- [ ] 已安裝 Claude Code 桌面版
- [ ] 電腦有網路連線

---

## 請 Claude Code 幫我執行以下步驟

> ⚠️ 以下內容是給 Claude Code 讀的操作指令。
> 你只需要把這整份 MD 檔丟給 Claude Code 桌面版的 Code 分頁，它會自動開始執行。
> 遇到需要你手動操作的地方，它會暫停並告訴你該怎麼做。
>
> **所有安裝與設定都在 Claude Code 桌面版內完成，不需要另外打開 PowerShell 或命令提示字元。**
> 如果 Claude Code 桌面版無法執行某個指令，才會引導你到終端機操作。
> 進階使用者也可以直接使用 Claude Code CLI 版本來執行本懶人包。

---

### 步驟零：環境檢查

> 請 Claude 在開始前，先自動確認以下所有項目。
> 如果有任何一項不符合，請先告知使用者問題所在，並引導解決後再繼續。
> **不要跳過任何一項檢查，不要假設環境正常。**

1. **確認作業系統**：執行系統指令確認是 Windows / macOS / Linux，後續所有指令請根據實際的作業系統選擇正確版本執行
2. **確認網路連線正常**：嘗試 ping 一個外部網站
3. **檢查 uv 是否已安裝**：執行 `uv --version`

> 把未安裝的項目記錄下來，後續步驟中會逐一安裝。
> 告知使用者目前的狀態，例如：「uv 未安裝 ❌，接下來會幫你安裝。」

---

### 步驟一：安裝 uv（如果未安裝）

> 如果步驟零確認 uv 已安裝，跳過此步驟。
> uv 是 Python 套件管理工具，後續安裝 MCP 工具會用到。

**Windows（PowerShell）**：
```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**macOS / Linux**：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

安裝完成後確認：
```bash
uv --version
```

> 如果 `uv` 指令找不到，請重開終端機再試一次。

---

### 步驟二：最終驗證

請逐一確認以下所有工具都能正常運作：

```bash
uv --version
```

> 全部通過後，告知使用者：
> 「✅ 環境建置完成！你的電腦已經準備好使用所有 Claude Code 懶人包了。」

---

## 如果安裝失敗，如何重來

對 Claude Code 說：
「環境建置懶人包執行失敗了，幫我檢查哪裡出問題，重新處理。」

Claude 會自動：
1. 重新執行步驟零的環境檢查
2. 找出失敗的項目
3. 針對失敗項目重新安裝或設定

---

## 常見問題

| 問題 | 解法 |
|------|------|
| winget 指令找不到 | Windows 10 1809 以上才有內建，或到 Microsoft Store 安裝「App Installer」 |
| brew 指令找不到（macOS） | 先安裝 Homebrew：`/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"` |
| 安裝後指令仍找不到 | 關閉所有終端機視窗，重新開啟後再試 |
| （實作後持續補充） | |

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-04-04 | v0.1 | 初版 |
| 2026-04-04 | v0.2 | 加入環境檢查、復原機制、跨平台支援 |
| 2026-06-12 | v0.4 | 移除 Git/GitHub CLI 安裝（改由 #02 負責），#00 只裝 uv |

---

## 相關連結

- [[01-連接 NotebookLM|懶人包 #01：連接 NotebookLM]]
- [[02-連接 GitHub|懶人包 #02：連接 GitHub]]
- [[Claude基本功EP03 - 懶人包先備工作]]
- [[README|Claude Code 懶人包索引]]
